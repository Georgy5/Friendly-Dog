<div class="container">
  <div class="row justify-content-center">
    <div class="col-6 p-5">
			<h2>Book <%= @dog.name %></h2>

			<%= render 'form' %>
			<br>
			<div class="btn-group">
				<%= link_to 'Back', dog_path(@dog), class: "btn btn-secondary btn-sm" %>
			</div>
		</div>
	</div>
</div>